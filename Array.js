// we can store num and obj in arrays
// for making react components we use arrays
// strings are immuteable arrays are mutables
let arr = [1,2,3,4,5,6,7,8,9]
//const cars = ["Saab","Volvo","BMW"];
const cars = [
    "Saab",
    "Volvo",
    "BMW",
];
console.log(arr[1]);
console.log(arr.toString());
// Using the JavaScript Keyword newThe following example also creates an Array, and assigns values to it
const car = new Array("Hi",2,3);
console.log(car[0]);
// const person = ["John", "Doe", 46];
// Objects use names to access its "members". In this example, person.firstName returns John:
// const person = {firstName:"John", lastName:"Doe", age:46};
// pop = shift
// push = unshift
// shift removing front of the array

// Function of the day Array.forEach()
// If you use named indexes, JavaScript will redefine the array to an object.
// Arrays with named indexes are called associative arrays (or hashes).
// JavaScript does not support arrays with named indexes.

//In JavaScript, arrays always use numbered indexes.
// JavaScript does not support associative arrays.
//You should use objects when you want the element names to be strings (text).
//You should use arrays when you want the element names to be numbers.
const point = new Array();
const points = [];




